/**
 * Default customer statuses matching the Prisma schema defaults.
 * Used as a fallback before settings are fetched from the API.
 */
export const DEFAULT_CUSTOMER_STATUSES = ['Evaluation', 'Implementation', 'Active', 'Suspended', 'Closed'];
